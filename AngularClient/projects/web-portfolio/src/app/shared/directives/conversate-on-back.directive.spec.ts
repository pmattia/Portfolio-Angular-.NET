import { ConversateOnBackDirective } from './conversate-on-back.directive';

describe('ConversateOnBackDirective', () => {
  it('should create an instance', () => {
    const directive = new ConversateOnBackDirective();
    expect(directive).toBeTruthy();
  });
});
